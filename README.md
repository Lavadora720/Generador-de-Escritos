# Generador-de-Escritos
Plantilla minimalista para generar cartas escritas como imagen en alta resolución. El contenido y la firma se configuran desde un JSON, el diseño visual desde un CSS intercambiable, y la exportación PNG se hace con un doble clic desde Windows usando Puppeteer en WSL.
